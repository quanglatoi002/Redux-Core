const initState = {
    filters: {
        search: '',
        status: 'All'
        priority: []
    },
    todoList: {},
};
const rootReducer = () => {
    return <div>reducer</div>;
};

export default rootReducer;
