const initState = {
    filters: {
        search: "",
        status: "All",
        priority: [],
    },
    todoList: [{ id: 1, name: "Learn PHP" }],
};
const rootReducer = () => {
    return <div>reducer</div>;
};

export default rootReducer;
